{
  "name": "deerflow-desktop",
  "version": "2.1.0",
  "private": true,
  "description": "DeerFlow desktop shell for Windows — Electron window that orchestrates the local Gateway API and Next.js frontend.",
  "author": "DeerFlow",
  "main": "main.js",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "electron .",
    "dev": "electron . --dev",
    "fetch-runtime": "node scripts/fetch-runtime.mjs",
    "build:frontend": "node scripts/build-frontend.mjs",
    "dist": "npm run fetch-runtime && npm run build:frontend && electron-builder --win nsis --x64",
    "dist:dir": "electron-builder --win --x64 --dir"
  },
  "devDependencies": {
    "electron": "^44.3.0",
    "electron-builder": "^26.15.3"
  }
}
